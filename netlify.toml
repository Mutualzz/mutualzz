[build]
base = "apps/app/src/renderer"
command = "vite build"
publish = "dist"

[functions]
directory = ".netlify/v1/functions"
node_bundler = "esbuild"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200

diff --git a/src/community/bottom-sheet/BottomSheet.android.tsx b/src/community/bottom-sheet/BottomSheet.android.tsx
index f60ae61bf0c9023d41c55cdca2d9af0de7291a92..f9a683e8b2329e853a5ec2958de19a1bbd00b8ff 100644
--- a/src/community/bottom-sheet/BottomSheet.android.tsx
+++ b/src/community/bottom-sheet/BottomSheet.android.tsx
@@ -89,6 +89,9 @@ export function BottomSheet(props: BottomSheetProps) {
   // Guards fireCloseCallbacks against double-firing when programmatic hide and
   // a native onDismissRequest race (e.g. swipe-dismiss during auto-close).
   const closedRef = useRef(indexProp < 0);
+  // While hide() is in flight, ignore onDismissRequest so we don't unmount the
+  // native sheet mid-animation (which flashes empty and skips the slide).
+  const programmaticHideRef = useRef(false);
 
   // Stable callback refs
   const onChangeRef = useRef(onChange);
@@ -106,20 +109,42 @@ export function BottomSheet(props: BottomSheetProps) {
     onChangeRef.current?.(-1);
   }, []);
 
+  const finishHide = useCallback(() => {
+    programmaticHideRef.current = false;
+    setIsOpen(false);
+    pendingIndexRef.current = null;
+    fireCloseCallbacks();
+  }, [fireCloseCallbacks]);
+
+  const startHide = useCallback(() => {
+    if (programmaticHideRef.current) return;
+    if (!sheetRef.current) {
+      finishHide();
+      return;
+    }
+    programmaticHideRef.current = true;
+    sheetRef.current.hide().then(finishHide, finishHide);
+  }, [finishHide]);
+
   // Sync with external index prop changes
   useEffect(() => {
     if (indexProp === -1) {
-      pendingIndexRef.current = null;
-      setIsOpen(false);
-      fireCloseCallbacks();
+      if (isOpenRef.current) {
+        startHide();
+      } else {
+        pendingIndexRef.current = null;
+        fireCloseCallbacks();
+      }
     } else if (indexProp >= 0) {
       pendingIndexRef.current = clampIndex(indexProp);
       closedRef.current = false;
+      programmaticHideRef.current = false;
       setIsOpen(true);
     }
-  }, [clampIndex, indexProp, fireCloseCallbacks]);
+  }, [clampIndex, indexProp, fireCloseCallbacks, startHide]);
 
   const handleDismiss = useCallback(() => {
+    if (programmaticHideRef.current) return;
     setIsOpen(false);
     fireCloseCallbacks();
   }, [fireCloseCallbacks]);
@@ -127,16 +152,13 @@ export function BottomSheet(props: BottomSheetProps) {
   const methods: BottomSheetMethods = useMemo(() => {
     const snapToIndex = (index: number) => {
       if (index === -1) {
-        sheetRef.current?.hide().then(() => {
-          setIsOpen(false);
-          pendingIndexRef.current = null;
-          fireCloseCallbacks();
-        });
+        startHide();
         return;
       }
       const clampedIndex = clampIndex(index);
       pendingIndexRef.current = clampedIndex;
       closedRef.current = false;
+      programmaticHideRef.current = false;
       if (!isOpenRef.current) {
         setIsOpen(true);
       } else if (hasMultipleSnapPoints) {
@@ -152,26 +174,18 @@ export function BottomSheet(props: BottomSheetProps) {
       onChangeRef.current?.(clampedIndex);
     };
 
-    const close = () => {
-      sheetRef.current?.hide().then(() => {
-        setIsOpen(false);
-        pendingIndexRef.current = null;
-        fireCloseCallbacks();
-      });
-    };
-
     return {
       snapToIndex,
       snapToPosition: (position: string | number) =>
         snapToIndex(findNearestSnapPointIndex(snapPointsProp, position)),
       expand: () => snapToIndex(maxIndex),
       collapse: () => snapToIndex(0),
-      close,
-      forceClose: close,
+      close: startHide,
+      forceClose: startHide,
       present: () => snapToIndex(0),
-      dismiss: close,
+      dismiss: startHide,
     };
-  }, [clampIndex, maxIndex, hasMultipleSnapPoints, fireCloseCallbacks, snapPointsProp]);
+  }, [clampIndex, maxIndex, hasMultipleSnapPoints, snapPointsProp, startHide]);
 
   useImperativeHandle(ref, () => methods, [methods]);
 
diff --git a/src/community/bottom-sheet/BottomSheet.ios.tsx b/src/community/bottom-sheet/BottomSheet.ios.tsx
index d9de442f1efd51749ff9a0e661e6979e7915fef1..def9ccb03e0d06b544e5d7ce1cff624cd55861ad 100644
--- a/src/community/bottom-sheet/BottomSheet.ios.tsx
+++ b/src/community/bottom-sheet/BottomSheet.ios.tsx
@@ -18,6 +18,7 @@ import {
   presentationDragIndicator,
   presentationSizing,
 } from '../../swift-ui/modifiers/presentationModifiers';
+import { ignoreSafeArea } from '../../swift-ui/modifiers';
 
 export { useBottomSheet } from './context';
 
@@ -165,10 +166,16 @@ export function BottomSheet(props: BottomSheetProps) {
   );
 
   const methods: BottomSheetMethods = useMemo(() => {
+    const close = () => {
+      setIsPresented(false);
+      setTimeout(() => {
+        fireCloseCallbacks();
+      }, 350);
+    };
+
     const snapToIndex = (index: number) => {
       if (index === -1) {
-        setIsPresented(false);
-        fireCloseCallbacks();
+        close();
         return;
       }
       const clampedIndex = Math.min(Math.max(index, 0), detents.length - 1);
@@ -179,16 +186,6 @@ export function BottomSheet(props: BottomSheetProps) {
       onChangeRef.current?.(clampedIndex);
     };
 
-    // Fire close callbacks immediately: the native `onIsPresentedChange` event is
-    // suppressed when JS drives the state change (the Swift layer guards against
-    // the feedback loop), so we can't rely on handlePresentedChange here. The
-    // closedRef guard inside fireCloseCallbacks prevents double-firing if a
-    // native user-dismiss event also arrives during the animation.
-    const close = () => {
-      setIsPresented(false);
-      fireCloseCallbacks();
-    };
-
     return {
       snapToIndex,
       snapToPosition: (position: string | number) =>
@@ -218,6 +215,7 @@ export function BottomSheet(props: BottomSheetProps) {
           ]),
       presentationDragIndicator(handleComponent === null ? 'hidden' : 'visible'),
       interactiveDismissDisabled(!enablePanDownToClose),
+      ignoreSafeArea({ edges: 'bottom' }),
       ...(bg ? [presentationBackground(bg)] : []),
     ];
   }, [
